<Window x:Class="Fluid.App.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:beh="clr-namespace:Fluid.App.Behaviors"
        Title="fluidMonitor — Help"
        Width="520" Height="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}"
        Style="{StaticResource BorderlessWindow}">
    <Grid>
        <TextBlock Text="Help" FontSize="13" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"
                   HorizontalAlignment="Left" VerticalAlignment="Top"
                   Margin="14,8,0,0" Panel.ZIndex="100"/>
        <Button Style="{StaticResource BorderlessCloseButton}"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="0,4,6,0" Panel.ZIndex="100"
                beh:WindowClose.IsClose="True"/>
        <ScrollViewer Margin="0,40,0,0" VerticalScrollBarVisibility="Auto"
                      Padding="22,8,22,18">
            <StackPanel x:Name="HelpContent"/>
        </ScrollViewer>
    </Grid>
</Window>
