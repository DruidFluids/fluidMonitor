<Window x:Class="Fluid.App.WindowPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:beh="clr-namespace:Fluid.App.Behaviors"
        Title="fluidMonitor — Pick a window"
        Width="400" Height="420"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="False"
        ResizeMode="NoResize"
        Background="{DynamicResource BackgroundBrush}"
        Style="{StaticResource BorderlessWindow}">
    <Grid>
        <TextBlock Text="Pick a window to block" FontSize="13" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextBrush}"
                   HorizontalAlignment="Left" VerticalAlignment="Top"
                   Margin="14,8,0,0" Panel.ZIndex="100"/>
        <Button Style="{StaticResource BorderlessCloseButton}"
                HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="0,4,6,0" Panel.ZIndex="100"
                beh:WindowClose.IsClose="True"/>
        <StackPanel Margin="22,52,22,18">
            <TextBlock Text="Select a window to add to the snap blocklist. The widget won't snap to windows matching the selected title."
                       Foreground="{DynamicResource MutedTextBrush}" FontSize="11"
                       TextWrapping="Wrap" Margin="0,0,0,10"/>
            <ListBox x:Name="WindowList" Height="260"
                     Background="{DynamicResource TileBrush}"
                     Foreground="{DynamicResource TextBrush}"
                     BorderBrush="{DynamicResource MutedTextBrush}"
                     BorderThickness="1" FontSize="12"
                     SelectionChanged="OnSelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Cancel" Style="{StaticResource BottomBar}" Margin="0,0,8,0"
                        beh:WindowClose.IsClose="True"/>
                <Button x:Name="AddBtn" Content="Add to blocklist" Style="{StaticResource BottomBarPrimary}"
                        Click="OnAdd" IsEnabled="False"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
